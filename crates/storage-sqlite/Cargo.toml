# SPDX-FileCopyrightText: Copyright (C) 2018-2023 Uwe Klotz <uwedotklotzatgmaildotcom> et al.
# SPDX-License-Identifier: AGPL-3.0-or-later

[package]
name = "aoide-storage-sqlite"
description = "aoide - Utilities for an SQLite storage backend"
version = { workspace = true }
license = { workspace = true }
edition = { workspace = true }
rust-version = { workspace = true }
publish = false

[dependencies]
anyhow = "1.0.70"
diesel = { version = "2.0.3", default-features = false, features = ["r2d2", "sqlite"] }
log = "0.4.17"
r2d2 = "0.8.10"
thiserror = "1.0.40"

# Feature "serde"
serde = { version = "1.0.160", optional = true, features = ["derive"] }

# Feature "sqlite-bundled"
# Bundle SQLite code to prevent dynamic linking.
# The pinned version must be compatible with diesel!
libsqlite3-sys = { version = "0.25.2", optional = true, features = ["bundled"] }

# Feature "tokio"
tokio = { version = "1.27.0", optional = true, default-features = false, features = ["macros", "rt", "sync", "time"] }

[features]
default = ["sqlite-bundled", "tokio"]
serde = ["dep:serde"]
sqlite-bundled = ["dep:libsqlite3-sys"]
tokio = ["dep:tokio"]
